# Feature Graph baseline — 2026-07-27

## Repository state

- Branch: `live-translations`
- Workspace commit: `bcd253b98bcfdd6983ec4701be9625077cc0ea52`
- Worktree after the workspace commit: clean
- No unrelated user changes overlap the planned refactor.

## Pre-refactor validation

| Command | Result |
| --- | --- |
| `./gradlew --quiet :feature-graph:testDebugUnitTest` | Passed |
| `./gradlew --quiet verifyEntryFeatureArchitecture` | Passed |

The architecture gate emitted only the JVM class-data-sharing warning. No test or validation failure was present before
the subject-scope refactor.

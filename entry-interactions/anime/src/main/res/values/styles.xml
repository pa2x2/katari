<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The video window remains black; Compose controls apply the selected app palette through TachiyomiTheme. -->
    <style name="Theme.AnimePlayer" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
